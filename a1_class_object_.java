/*
	 *    CLASS :-
	 * ~> template for object
	 * ~> blueprint for creating object
	 * ~> it helps us to generate object using "new" keyword
	 * 
	 *    OBJECT :-
	 * ~> instance of a class
	 * ~> it is a real world entity that has an identity, behavior, state. 
*/
public class class_object {
	public static void main(String[] args) {
		class_object a1 = new class_object();
		System.out.println(a1);
		
		// it will print add of reference variable or object add
	}
}
