/*
 * 		LAMBDA EXPRESSION :-
 *
 * ~> we can reduce the number of lines of code
 * ~> there is no need of creating object
 * ~> you can use that in certain place
 * 		like -> functional interface
 * ~> object creation are not required to access 
 * 		non-static members
 * ~> that's why we call as functional programming
 * 		language (since Java 8)
*/

@FunctionalInterface
public interface lambda_expression_1 {
	public void test();
}
